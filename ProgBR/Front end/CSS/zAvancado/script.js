/*------------------------------------------------------------------------------------------ Seletores */

// With querySelector you select only the first element with those selector
var firstTitle = document.querySelector('[title]')

// With querySelectorAll you select all elements with those selectors

var allTitles = document.querySelectorAll('[title]')

console.log(firstTitle)
console.log(allTitles)